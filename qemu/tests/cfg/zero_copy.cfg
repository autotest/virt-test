- zero_copy:
    only Host_Fedora
    only virtio_net
    type = zero_copy
    start_vm = no
    kill_vm = yes
    variants:
        - enable:
            enable_zerocp = yes
        - disable:
            enable_zerocp = no
