- numa:
    type = numa_basic
    vms = ""
    kill_vm_on_error = yes
    login_timeout = 240
    variants:
        - numa_basic:

        - numa_consistency:
            only Linux
            type = numa_consistency
            start_vm = no
