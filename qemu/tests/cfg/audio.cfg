- audio:
    only Linux
    no RHEL.3.9
    testcase = 'audio'
    type = audio
    soundcards = ac97
    random_content_size = 100K
    audio_test_cmd = test -c /dev/dsp && dd if=/dev/urandom of=/dev/dsp bs=%s count=1

