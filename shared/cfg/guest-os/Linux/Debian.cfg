- Debian:
    shell_prompt = "^.*@.*[\#\$]\s*$"
    password = 12345678
    image_name = images/debian
    unattended_install, svirt_install:
        kernel = linux
        initrd = initrd
        wait_no_ack = yes
        kernel_params = "console=ttyS0,115200 console=tty0"
