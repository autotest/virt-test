- nmi_watchdog: install setup image_copy unattended_install.cdrom
    virt_test_type = kvm
    type = nmi_watchdog
    get_nmi_cmd = grep NMI /proc/interrupts
    nmi_watchdog_type = 1
    image_snapshot = yes
    only Linux
