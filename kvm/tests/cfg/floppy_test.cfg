- floppy_test: install setup image_copy unattended_install.cdrom
    virt_test_type = kvm
    only Linux
    type = floppy
    start_vm = no
    floppies = "fl"
    floppy_name = images/test_floppy.img
