- set_link: install setup image_copy unattended_install.cdrom
    virt_test_type = kvm
    type = set_link
    test_timeout = 1000
    filesize = 4000
    transfer_timeout = 1000
    transfer_type = remote
    kill_vm =yes
