- qmp_basic: install setup image_copy unattended_install.cdrom
    virt_test_type = kvm
    type = qmp_basic
    monitors = qmp1
    monitor_type = qmp
    vcpu_thread_pattern = "u'thread_id':\s+(\d+)"
