- zero_copy:
    no Host_RHEL.5, Host_RHEL.6
    only virtio_net
    type = zero_copy
    start_vm = no
    kill_vm = yes
    variants:
        - enable:
            enable_zerocp = yes
        - disable:
            enable_zerocp = no
