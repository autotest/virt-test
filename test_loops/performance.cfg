variants:
    - @prepare:
        only unattended_install.cdrom.default_install.aio_native rh_kernel_update disable_win_update
        case_type = prepare
    - @block:
        only iometer_performance qcow2 iozone_windows performance
    - @nic:
        only guest_test.autoit.web_bench
    - @kernel:
        only kernbench kernbench2
    - @cpu:
        only guest_test.autoit.super_pi guest_test.autoit.sandra
    - @image_check:
        only qemu_img_check
