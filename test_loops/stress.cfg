variants:
    - @prepare:
        only unattended_install.cdrom.default_install.aio_native rh_kernel_update disable_win_update
        case_type = prepare
    - @mem_stress:
        only stress_boot
    - @block_stress:
        only yonit_bitmap_benchmark
    - @cpu_sress:
        only stress_kernel_compile softlockup

test_timeout = 28800
